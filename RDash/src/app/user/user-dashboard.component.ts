import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-user-dashboard',
  template: `
    <h2>User Dashboard</h2>
    <p>Your assigned tasks will appear here (feature in next phase).</p>
  `
})
export class UserDashboardComponent { }
